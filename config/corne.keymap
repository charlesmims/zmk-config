/*
 * Copyright (c) 2020 The ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>


&lt {
        tapping-term-ms = <200>;
        quick-tap-ms = <500>;
};
&mt {
        quick-tap-ms = <200>;
};
/ {
        behaviors {
                hm:     homerow_mods {
                        compatible = "zmk,behavior-hold-tap";
                        #binding-cells = <2>;
                        tapping-term-ms = <200>;    // <---[[moderate duration]]
                        quick-tap-ms = <100>;
                        require-prior-idle-ms = <200>;
                        flavor = "tap-preferred";
                        bindings = <&kp>, <&kp>;
                };
                am: alt_mods {
                        compatible = "zmk,behavior-hold-tap";
                        #binding-cells = <2>;
                        tapping-term-ms = <500>;    // <---[[longer duration]]
                        quick-tap-ms = <100>;
                        require-prior-idle-ms = <500>;
                        flavor = "tap-preferred";
                        bindings = <&kp>, <&kp>;
                };

        };
        keymap {
                compatible = "zmk,keymap";

                default_layer {
// -----------------------------------------------------------------------------------------
// |  ESC |  Q  |  W  |  F  |  p  |  b  |   |  j  |  l   |  u  |  y  |  ;  | ALT-GRAVE |
// | TAB |  A  |  r  |  s  |  t  |  G  |   |  m  |  n   |  e  |  i  |  o  |  '   |
// | SHFT |  Z  |  X  |  C  |  d  |  v  |   |  k  |  h   |  ,  |  .  |  /  |   SHFT    |
//                    | RAISE | CMD | lower_BKSP |   | raise_SPC | lower_ENTER  | CTRL |

                        display-name = "Base";
                        bindings = <
   &kp ESC   &kp Q &kp W &kp F &kp P &kp B   &kp J &kp L  &kp U     &kp Y   &kp SEMI    &mt RALT GRAVE
   &kp TAB &kp A &kp R &kp S &kp T &kp G   &kp M &kp N  &kp E     &kp I   &kp O       &kp SQT
   &kp LSHFT &kp Z &kp X &kp C &kp D &kp V   &kp K &kp H  &kp COMMA &kp DOT &kp FSLH &kp RSHFT
                  &mo 2 &kp LGUI &hm 1 BKSP   &hm 2 SPACE &hm 1 ENTER &kp RCTRL
                        >;
                };
                lower_layer {
// -----------------------------------------------------------------------------------------
// |      |  1  |  2  |  3  |  4  |  5  |   |  6  |  7  |  8  |  9  |  0  |        |
// |      |     |     |     |     |     |   |     | LFT |  DWN | UP | RGT |        |
// |      |BT1 | BT2 | BT3 | BT4 | BT5 |   |     | HOME| PGDN |PGUP| END  | BTCLR  |
//                    | RAISE | LWR | BKSP|   | SPC | ENTER  | RAISE |
                        display-name = "Lower";
                        bindings = <
        &trans   &kp N1  &kp N2  &kp N3  &kp N4  &kp N5      &kp N6     &kp N7    &kp N8   &kp N9    &kp N0   &trans
        &trans   &trans  &trans  &trans  &trans  &trans   &trans   &kp LEFT &kp DOWN &kp UP &kp RIGHT &trans
        &trans &bt BT_SEL 0 &bt BT_SEL 1 &bt BT_SEL 2 &bt BT_SEL 3 &bt BT_SEL 4     &trans &kp HOME &kp PG_DN &kp PG_UP &kp END &bt BT_CLR
                  &mo 2 &kp LGUI &hm 1 BKSP   &hm 2 SPACE &hm 1 ENTER &kp RCTRL
                        >;
                };

                raise_layer {
// -----------------------------------------------------------------------------------------
// |         |  !  |  @  |  #  |  $  |  %        |||     |  ^  |  &  |  *  |  (  |  )  |      |
// |  vol up | prev | next |     |              |||     |  -  |  =  |  [  |  ]  |  \  |      |
// |  vol dn | play |BRI_DN| BRI_UP |     |     |||     |  _  |  +  |  {  |  }  | "|" |      |
//                     | RAISE | LWR | BKSP|   | SPC | ENTER  | RAISE |
                        display-name = "Raise";
                        bindings = <
   &trans  &kp EXCL &kp AT &kp HASH &kp DLLR &kp PRCNT   &kp CARET &kp AMPS  &kp KP_MULTIPLY &kp LPAR &kp RPAR &trans
   &kp C_VOL_UP  &kp C_PREV  &kp C_NEXT &trans   &trans   &trans      &kp MINUS &kp EQUAL &kp LBKT        &kp RBKT &kp BSLH &trans
   &kp C_VOL_DN  &kp C_PP   &kp C_BRI_DN &kp C_BRI_UP   &trans   &trans      &kp UNDER &kp PLUS  &kp LBRC        &kp RBRC &kp PIPE &trans
                  &mo 2 &kp LGUI &hm 1 BKSP   &hm 2 SPACE &hm 1 ENTER &kp RCTRL
                            >;
                };
        };
};
